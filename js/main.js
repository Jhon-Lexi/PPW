import { initRouter } from './router.js';

document.addEventListener('DOMContentLoaded', () => {
    // Inicializar el Router
    initRouter();
    
    console.log("⚡ Los circuitos del tiempo están encendidos. ¡App lista!");
});